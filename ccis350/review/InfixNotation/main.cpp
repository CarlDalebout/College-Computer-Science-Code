#include <iostream>
#include <forward_list>
#include <string>

int main()
{
    std::forward_list<char> opestk;
    std::forward_list<int> intstk;
    std::string input;

    std::getline(std::cin, input);
    int i = 0;
    while(input[i] != '\0')
    {
        if(input[i+i] == ' ')
        {
            
        }
    }

    return 0;
}